1. Multer: Multer is a node.js middleware for handling multipart/form-data, which is primarily used for uploading files.

2. Context API is a (kind of) new feature added in version 16.3 of React that allows one to share state across the entire app (or part of it) lightly and with ease. Let's see how to use it.

3. .env Environment variables can also help us to store our API link in one location so that we don’t have to change the link in each file manually. It is used to store sensitive credentials like API keys.

4. process.env -> it is used to fetch data from env file.
